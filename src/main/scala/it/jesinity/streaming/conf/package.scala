package it.jesinity.streaming

/**
  * created by CGnal s.p.a
  */
package object conf {

  val `bootstrap.servers` = "bootstrap.servers"
  val `key.serializer`    = "key.serializer"
  val `value.serializer`  = "value.serializer"
  val `users`             = "users"
  val `startingDay`       = "startingDay"
  val `topic.transaction` = "topic.transaction"
  val `topic.wordcount`   = "topic.wordcount"

}
